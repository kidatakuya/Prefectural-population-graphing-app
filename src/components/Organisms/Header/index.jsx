// import { Title } from '../../index';
import React from 'react';
import './index.scss';

function Header({ children }) {
  return (
    <header>
      {children}
    </header>
  );
}

export default Header;
